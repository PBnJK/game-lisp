(import game)

(let x 32)
(let y 200)

(while true (
  (if (and (> y 0) (is_key_pressed "KeyW")) (
    (= y (- y 0.5))
  ))
  (if (and (< y 416) (is_key_pressed "KeyS")) (
    (= y (+ y 0.5))
  ))

  (clear)
  (draw_rect x y 16 64)
))
